# SQL_DATA_CLEANING

This project focuses on cleaning and analyzing a simulated pizza delivery database. 
The raw data contained significant "noise"—including inconsistent null values and mixed data types (strings vs. numbers)—which required T-SQL transformation before meaningful business insights could be extracted.
